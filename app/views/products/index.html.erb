<p id="notice"><%= notice %></p>

<h1>Urban Renewal</h1>


<table class="product-table striped responsive-table">
  <!-- <thead align="left">
    <tr>
      <th></th>
      <th>Item</th>
      <th>Description</th>
      <th>Size</th>
      <th>Brand</th>
      <th>Color</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead> -->

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= image_tag product.image.url(:medium) %></td>
        <td><%= product.name %></td>
        <td><%= product.description %></td>
        <td><%= product.size %></td>
        <td><%= product.brand %></td>
        <td><%= product.color %></td>
        <td>$<%= product.price_in_cents %></td>
        <td><%= link_to 'Show', product , class: "main btn-flat"%></td>
        <td><%= link_to 'Edit', edit_product_path(product),class: "main btn-flat"%></td>
        <td><%= link_to 'Destroy', product, class: "main btn-flat", method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product', new_product_path, class: "new-product btn-flat" %>
